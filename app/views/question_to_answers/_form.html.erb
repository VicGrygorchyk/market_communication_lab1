<%= form_with(model: question_to_answer) do |form| %>
  <% if question_to_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_to_answer.errors.count, "error") %> prohibited this question_to_answer from being saved:</h2>

      <ul>
        <% question_to_answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.text_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :answer_id %>
    <%= form.text_field :answer_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
